---
title: "Einführung in R & RStudio – Tag 2"
subtitle: "Fehlende Werte, Umkodieren & Workflows"
format:
  html:
    toc: true
    toc-title: "Tag 2 – Inhalte"
    toc-location: left
    toc-depth: 2

    
sidebar: workshop1
---

Willkommen zurück 👋  
Wir starten mit einem kurzen Recap von Tag 1 und klären offene Fragen.

# Recap - Tag 1

Wir haben im ersten Teil des Workshops gelernt, wie RStudio aufgebaut ist und funktioniert. Wir kennen nun die wichtigsten Grundlagen und können diese heute weiter **vertiefen und festigen**.

::: callout-tip
# **Recap-Fragen**
- Was ist ein Data Frame?
- Was macht `select()` vs. `filter()`?
- Wofür nutzen wir Pipes (`|>`)?
:::

::: callout-note
# **Merksatz**

R ist eine **Programmiersprache** – und wie jede Sprache lernt man sie  
durch **regelmäßige Nutzung**.

Fehler zu machen ist völlig normal (auch wenn es manchmal frustrierend ist).  
Für fast jedes Problem gibt es **eine Lösung, eine Erklärung oder ein Paket** 😊
:::

___

# Umkodieren

In der Datenanalyse arbeiten wir häufig mit Variablen,  
deren Werte **nicht direkt so vorliegen**, wie wir sie für eine Auswertung benötigen.

**Umkodieren** bedeutet, bestehende Variablen so zu verändern oder neu zu definieren,  
dass sie **inhaltlich sinnvoll** und **analytisch nutzbar** werden.

Beim Umkodieren können zum Beispiel:

- Kategorien zusammengefasst werden  
  (z. B. mehrere Bildungsabschlüsse → „niedrig / mittel / hoch“)
- Werte umgedreht werden  
  (z. B. hohe Werte = geringe Zustimmung)
- neue Variablen aus bestehenden gebildet werden  
  (z. B. Summen- oder Skalenwerte) **→** das kennen wir schon!

::: callout-tip
# **Warum ist Umkodieren wichtig?**

Rohdaten sind selten sofort „analysefertig“.  
Durch Umkodieren stellen wir sicher,  
dass unsere Variablen **inhaltlich korrekt** interpretiert  
und **vergleichbar** ausgewertet werden können. Umkodierung ist daher ein wichtiger **Bestandteil der Datenaufbereitung** und **wichtig** für die späteren Analysen!
:::

Im Folgenden sehen wir typische Beispiele für Umkodierungen  
und setzen sie direkt mit R um.

## Umkodieren invertierter Items

In Fragebögen kommen häufig invertierte Items vor. Das bedeutet: Hohe Werte drücken geringe Zustimmung aus – oder umgekehrt.

Damit alle Items in dieselbe Richtung zeigen, müssen invertierte Items umkodiert werden.

### Beispiel 

Die Items `hs04` bis `hs05` Fragen die mentale Gesundheit in den letzten vier Wochen ab:

|**Variable**|**Inhalt**|**Skalierung**|
|___|___|___|
|hs04|"Hetze, unter Zeitdruck"| 1 = "Immer"
bis
5 = "Nie"|




# Fehlende Werte (NA)

Warum fehlende Werte wichtig sind, wie man sie erkennt und sinnvoll behandelt.

___

# Umkodieren

Wie wir Variablen neu kodieren (z. B. Kategorien zusammenfassen, Werte umdrehen, Skalen bilden).

___

# Joins

Wie wir Datensätze über eine gemeinsame ID zusammenführen (z. B. `left_join()`).

___

# Reproduzierbare Workflows

Ordnerstruktur, Skripte, klare Dateinamen, Outputs – damit Analysen nachvollziehbar bleiben.

___

# Mini-Projekt

Wir kombinieren alles: Import → Check → Missing → Recodes → Join → Export.
